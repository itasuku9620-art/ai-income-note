services:
  - type: web
    name: ai-income-note
    env: node
    buildCommand: npm install
    startCommand: node app/server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
